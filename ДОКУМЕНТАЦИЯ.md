# 📚 Документация: Микросервис векторного поиска

## 📋 Оглавление

1. [Описание проекта](#описание-проекта)
2. [Архитектура](#архитектура)
3. [Требования](#требования)
4. [Установка и запуск](#установка-и-запуск)
5. [API Endpoints](#api-endpoints)
6. [Примеры использования](#примеры-использования)
7. [Тестирование](#тестирование)
8. [Передача проекта коллеге](#передача-проекта-коллеге)
9. [Устранение неполадок](#устранение-неполадок)

---

## 📖 Описание проекта

**Vector Search Microservice** - микросервис для семантического поиска по таблицам базы данных PostgreSQL с использованием:
- Векторного поиска (pgvector)
- LLM-ререанкинга (Ollama)
- Гибридного поиска (RRF: Vector + BM25/Tags)

### Основные возможности:

✅ Векторный поиск по описаниям таблиц
✅ LLM-ререранкинг для улучшения качества
✅ Гибридный поиск (комбинация методов)
✅ Поддержка различных метрик расстояния (cosine, L2, dot_product)
✅ Батчинг для оптимизации работы LLM

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     FastAPI Application                      │
├─────────────────────────────────────────────────────────────┤
│  Endpoints:                                                  │
│  • /search              - Простой векторный поиск           │
│  • /search_rerank       - Векторный поиск + LLM ререранкинг │
│  • /search_hybrid       - Гибридный (Vector + Tags)         │
│  • /search_hybrid_bm25  - Гибридный (Vector + BM25)         │
└─────────────────────────────────────────────────────────────┘
           │                    │                    │
           ▼                    ▼                    ▼
    ┌──────────┐        ┌──────────┐        ┌──────────┐
    │PostgreSQL│        │  Ollama  │        │   RRF    │
    │ pgvector │        │Embedding │        │ Fusion   │
    └──────────┘        │  Gemma3  │        └──────────┘
                        └──────────┘
```

### Компоненты:

- **app/main.py** - FastAPI приложение, роутинг
- **app/search.py** - Векторный поиск
- **app/rerank.py** - LLM ререранкинг с батчингом
- **app/hybrid_search.py** - Гибридный поиск (Vector + Tags embedding)
- **app/hybrid_search_bm25.py** - Гибридный поиск (Vector + BM25)
- **app/embedding.py** - Генерация эмбеддингов через Ollama
- **app/database.py** - Подключение к PostgreSQL
- **app/models.py** - Pydantic модели запросов/ответов

---

## 💻 Требования

### Системные требования:

- **Python**: 3.12+
- **PostgreSQL**: 14+ с расширением `pgvector`
- **Ollama**: для генерации эмбеддингов и LLM ререранкинга

### База данных:

**Таблица:** `tables_metadata`

**Структура:**
```sql
CREATE TABLE tables_metadata (
    table_name TEXT PRIMARY KEY,
    table_description TEXT,
    description_embedding vector(1024),  -- эмбеддинг описания
    tags JSONB,                          -- {"keywords": "crimes, location, ..."}
    tags_embedding vector(1024)          -- эмбеддинг тегов
);
```

**Подключение:**
- Host: `your_database_host:5432`
- Database: `your_database_name`
- User: `your_db_user`

### Ollama сервер:

- **Embedding модель**: `dengcao/Qwen3-Embedding-0.6B:Q8_0`
- **LLM модель**: `gemma3:27b`
- **Host**: `http://your_ollama_host:11434`

---

## 🚀 Установка и запуск

### 1. Клонирование/получение проекта

```bash
# Проект находится в
cd /home/keosido/Desktop/Vector_search
```

### 2. Создание виртуального окружения

```bash
# Создать виртуальное окружение
python3 -m venv vector_search_env

# Активировать
source vector_search_env/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

**requirements.txt:**
```txt
fastapi==0.115.12
uvicorn[standard]==0.34.2
asyncpg==0.30.0
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
ollama==0.4.7
httpx==0.25.2
```

### 4. Настройка конфигурации

Файл **`.env`** уже настроен:

```env
# Database
DB_NAME=your_database_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=your_database_host
DB_PORT=5432

# Ollama
OLLAMA_HOST=http://your_ollama_host:11434
OLLAMA_RERANK_HOST=http://your_ollama_host:11434
EMBEDDING_MODEL=dengcao/Qwen3-Embedding-0.6B:Q8_0
RERANK_MODEL=gemma3:27b

# API
API_HOST=0.0.0.0
API_PORT=8000
```

### 5. Запуск сервера

```bash
# Запуск с auto-reload (для разработки)
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# Запуск в production (без reload)
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**Сервер будет доступен:**
- Локально: `http://localhost:8000`
- В сети: `http://10.10.4.38:8000` (ваш IP)
- Документация: `http://localhost:8000/docs`

---

## 🔌 API Endpoints

### 1. `/search` - Простой векторный поиск

**Описание:** Быстрый векторный поиск по эмбеддингам описаний таблиц.

**Метод:** `POST`

**Параметры:**
```json
{
  "query": "текст запроса",
  "top_n": 10,
  "method": "cosine"
}
```

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `query` | string | обязательно | Поисковый запрос |
| `top_n` | int | 10 | Количество результатов (1-10000) |
| `method` | string | "cosine" | Метрика: "cosine", "L2", "dot_product" |

**Ответ:**
```json
{
  "results": [
    {
      "table_name": "crimes",
      "score": 0.8523
    }
  ],
  "total": 10
}
```

**Скорость:** ~100-200ms
**Точность:** Средняя (зависит от качества эмбеддингов)

---

### 2. `/search_rerank` - Векторный поиск + LLM ререранкинг

**Описание:** Векторный поиск с последующим ререранкингом через LLM для максимальной точности.

**Метод:** `POST`

**Параметры:**
```json
{
  "query": "Сколько преступлений в 2020 году?",
  "prompt": "Find the most relevant database tables for this query",
  "top_n": 50,
  "method": "dot_product"
}
```

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `query` | string | обязательно | Поисковый запрос |
| `prompt` | string | "Find the most..." | Контекст для LLM |
| `top_n` | int | 50 | Кандидаты для ререранкинга (1-10000) |
| `method` | string | "cosine" | Метрика векторного поиска |

**Ответ:**
```json
{
  "results": [
    {
      "table_name": "crimes",
      "score": 0.95
    }
  ],
  "total": 50
}
```

**Особенности:**
- ✅ **Батчинг**: Кандидаты обрабатываются по 10 таблиц за раз
- ✅ **Оптимизация**: Query/Context размещены в конце промпта (recency bias)
- ✅ **Скорость**: 2-5 секунд для top_n=50 (5 батчей)
- ✅ **Точность**: Высокая (TOP_1: 27.5% на бенчмарках)

**Размер батча:** 10 таблиц (задан в `app/rerank.py`, строка 164: `BATCH_SIZE = 10`)

---

### 3. `/search_hybrid` - Гибридный поиск (Vector + Tags embedding)

**Описание:** RRF комбинация векторного поиска и поиска по эмбеддингам тегов.

**Метод:** `POST`

**Параметры:**
```json
{
  "query": "преступления в районах",
  "top_n": 10,
  "vector_weight": 0.5,
  "tags_weight": 0.5,
  "method": "cosine",
  "rrf_k": 60
}
```

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `query` | string | обязательно | Поисковый запрос |
| `top_n` | int | 10 | Итоговое количество (1-100) |
| `vector_weight` | float | 0.5 | Вес векторного поиска (0.0-1.0) |
| `tags_weight` | float | 0.5 | Вес поиска по тегам (0.0-1.0) |
| `method` | string | "cosine" | Метрика векторного поиска |
| `rrf_k` | int | 60 | RRF константа (1-100) |

**Ответ:**
```json
{
  "results": [
    {
      "table_name": "crimes",
      "rrf_score": 0.0163,
      "vector_rank": 2,
      "tags_rank": 1
    }
  ],
  "total": 10
}
```

**Примечание:** По результатам тестов, tags_embedding ухудшает результаты. Рекомендуется использовать `/search_rerank`.

---

### 4. `/search_hybrid_bm25` - Гибридный поиск (Vector + BM25)

**Описание:** RRF комбинация векторного поиска и BM25 полнотекстового поиска по тегам.

**Метод:** `POST`

**Параметры:** Идентичны `/search_hybrid`

**Особенности:**
- Использует PostgreSQL full-text search
- BM25 ранжирование по полю `tags->>'keywords'`
- Поддержка русского языка (`to_tsvector('russian', ...)`)

**Статус:** ⚠️ В разработке (возможны ошибки с SQL запросами)

---

### 5. `/health` - Проверка работоспособности

**Метод:** `GET`

**Ответ:**
```json
{
  "status": "healthy",
  "database": "connected"
}
```

---

## 📝 Примеры использования

### Пример 1: Простой поиск

```bash
curl -X POST "http://localhost:8000/search" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Сколько преступлений зарегистрировано?",
    "top_n": 5,
    "method": "dot_product"
  }'
```

### Пример 2: Поиск с LLM ререранкингом

```bash
curl -X POST "http://localhost:8000/search_rerank" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Какие улицы в 5-м районе?",
    "top_n": 50,
    "method": "dot_product"
  }'
```

### Пример 3: Python клиент

```python
import requests

url = "http://localhost:8000/search_rerank"
payload = {
    "query": "Сколько лесных массивов в 4-м районе?",
    "top_n": 50,
    "method": "dot_product"
}

response = requests.post(url, json=payload)
results = response.json()

for idx, result in enumerate(results['results'][:5], 1):
    print(f"{idx}. {result['table_name']} - score: {result['score']:.3f}")
```

---

## 🧪 Тестирование

### Бенчмарк тесты

В проекте есть готовые скрипты для тестирования качества поиска:

#### 1. Тест `/search_rerank` (рекомендуется)

```bash
python test_benchmark.py
```

**Результаты (top_n=100, method=dot_product):**
- TOP_1: 27.5% (14/51)
- TOP_3: 31.4% (16/51)
- NOT_FOUND: 3.9% (2/51)
- Средняя позиция: 5.51

#### 2. Тест `/search_hybrid`

```bash
python test_hybrid_simple.py
```

**Результаты (vector=0.5, tags=0.5):**
- TOP_1: 5.9% (3/51)
- NOT_FOUND: 15.7% (8/51)
- Средняя позиция: 12.81

#### 3. Тест `/search_hybrid_bm25`

```bash
python test_bm25_benchmark.py
```

**Статус:** ⚠️ Не работает (ошибки SQL)

### Структура benchmarks.json

```json
[
  {
    "question": "Сколько преступлений зарегистрировано в 2020 году?",
    "target_table": "crimes",
    "sql_query": "SELECT COUNT(*) FROM crimes WHERE year = 2020;",
    "answer": "4897"
  }
]
```

**Всего тестов:** 51

---

## 📦 Передача проекта коллеге

### Вариант 1: Передача через архив

```bash
# Создать архив проекта
cd /home/keosido/Desktop
tar -czf vector_search.tar.gz Vector_search/

# Исключить виртуальное окружение и кеш
tar -czf vector_search.tar.gz \
  --exclude='Vector_search/vector_search_env' \
  --exclude='Vector_search/__pycache__' \
  --exclude='Vector_search/**/__pycache__' \
  --exclude='Vector_search/**/*.pyc' \
  Vector_search/
```

**Передать файл:** `vector_search.tar.gz`

### Вариант 2: Передача через Git

```bash
cd /home/keosido/Desktop/Vector_search

# Инициализировать git
git init

# Создать .gitignore
cat > .gitignore << 'EOF'
vector_search_env/
__pycache__/
*.pyc
*.pyo
.env
*.json
*.backup
EOF

# Закоммитить
git add .
git commit -m "Initial commit: Vector Search Microservice"

# Опционально: push в репозиторий
# git remote add origin <URL>
# git push -u origin main
```

### Вариант 3: Копирование на сервер

```bash
# С вашей машины на машину коллеги
scp -r /home/keosido/Desktop/Vector_search user@colleague-ip:/path/to/destination
```

---

## 📋 Инструкция для коллеги

### Шаг 1: Распаковка проекта

```bash
# Если получен архив
tar -xzf vector_search.tar.gz
cd Vector_search
```

### Шаг 2: Установка зависимостей

```bash
# Создать виртуальное окружение
python3 -m venv vector_search_env

# Активировать
source vector_search_env/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 3: Проверка конфигурации

Убедиться что `.env` файл настроен правильно:

```bash
cat .env
```

**Важно проверить:**
- ✅ Доступ к PostgreSQL (your_database_host:5432)
- ✅ Доступ к Ollama (your_ollama_host:11434)
- ✅ Модели Ollama установлены

### Шаг 4: Тестовый запуск

```bash
# Проверить health
curl http://localhost:8000/health

# Если OK - сервер работает!
```

### Шаг 5: Первый запрос

```bash
curl -X POST "http://localhost:8000/search" \
  -H "Content-Type: application/json" \
  -d '{"query": "преступления", "top_n": 3}'
```

---

## 🔧 Устранение неполадок

### Ошибка: "Database connection failed"

**Проблема:** Нет доступа к PostgreSQL

**Решение:**
```bash
# Проверить доступность базы
psql -h your_database_host -U your_db_user -d your_database_name

# Проверить .env файл
cat .env | grep DB_
```

### Ошибка: "Ollama connection failed"

**Проблема:** Ollama сервер недоступен

**Решение:**
```bash
# Проверить доступность Ollama
curl http://your_ollama_host:11434/api/tags

# Проверить модели
curl http://your_ollama_host:11434/api/show -d '{"name":"gemma3:27b"}'
```

### Ошибка: "Module not found"

**Проблема:** Зависимости не установлены

**Решение:**
```bash
# Активировать виртуальное окружение
source vector_search_env/bin/activate

# Переустановить зависимости
pip install -r requirements.txt
```

### Сервер не отвечает

**Проблема:** Порт 8000 занят или firewall блокирует

**Решение:**
```bash
# Проверить порт
netstat -tuln | grep 8000

# Или использовать другой порт
uvicorn app.main:app --host 0.0.0.0 --port 8001
```

### Медленная работа `/search_rerank`

**Проблема:** LLM модель перегружена

**Решение:**
- Уменьшить `top_n` (рекомендуется 50)
- Увеличить `BATCH_SIZE` в `app/rerank.py` (осторожно!)
- Использовать более легкую LLM модель

---

## 📊 Рекомендации по использованию

### Когда использовать какой эндпойнт?

| Эндпойнт | Скорость | Точность | Случай использования |
|----------|----------|----------|----------------------|
| `/search` | ⚡⚡⚡ Быстро (100ms) | 🎯 Средняя | Быстрый поиск, автодополнение |
| `/search_rerank` | ⚡ Медленно (2-5s) | 🎯🎯🎯 Высокая | Production, точные результаты |
| `/search_hybrid` | ⚡⚡ Средне (500ms) | 🎯 Низкая | ❌ Не рекомендуется |
| `/search_hybrid_bm25` | ⚡⚡ Средне | ❓ Неизвестно | ⚠️ В разработке |

### Оптимальные параметры

**Для `/search_rerank`:**
- `top_n`: 50-100
- `method`: "dot_product"
- `prompt`: оставить по умолчанию

**Для `/search`:**
- `top_n`: 10-20
- `method`: "cosine" или "dot_product"

---

## 📞 Контакты

**Автор:** keosido
**Локация проекта:** `/home/keosido/Desktop/Vector_search`
**IP в сети:** `10.10.4.38:8000`

---

## 📜 История изменений

**v1.0** (текущая версия)
- ✅ Простой векторный поиск
- ✅ LLM ререранкинг с батчингом (BATCH_SIZE=10)
- ✅ Гибридный поиск (Vector + Tags)
- ✅ Гибридный поиск (Vector + BM25) - в разработке
- ✅ Оптимизация промпта (Query/Context в конце)
- ✅ Поддержка 3 метрик расстояния
- ✅ Benchmark тесты

**Известные проблемы:**
- ⚠️ `/search_hybrid_bm25` не работает (SQL ошибки)
- ⚠️ Tags embedding ухудшает результаты
- ⚠️ Рекомендуется использовать только `/search_rerank`

---

**🎯 Лучший результат: `/search_rerank` с top_n=100 и method=dot_product**
