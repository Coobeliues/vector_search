# 🔐 Безопасность и конфиденциальность данных

## ⚠️ КРИТИЧНО: Секретные данные в проекте

Перед публикацией в GitHub или передачей третьим лицам, убедись что удалены:

### 📁 Файлы с секретами:

1. **`.env`** - пароли, IP адреса, учетные данные ✅ (уже в .gitignore)
2. **`benchmarks.json`** - реальные данные БД ✅ (уже в .gitignore)
3. **`*_results.json`** - результаты тестов ✅ (уже в .gitignore)

### 📝 Документация:

**ВНИМАНИЕ!** Следующие файлы содержат примеры с реальными данными:

- ❌ `ДОКУМЕНТАЦИЯ.md` - содержит реальные IP, логины, пароли
- ❌ `БЫСТРЫЙ_СТАРТ.md` - содержит реальные учетные данные

**Решение:** Создай отдельные версии для GitHub (см. ниже)

### 💻 Код:

- `app/config.py` - содержит дефолтные IP адреса (не критично, но лучше убрать)

---

## 🛡️ Подготовка к публикации в GitHub

### Вариант 1: Две версии документации (РЕКОМЕНДУЕТСЯ)

**Внутренняя версия** (с реальными данными):
- `ДОКУМЕНТАЦИЯ.md` → оставить как есть для внутреннего использования
- `БЫСТРЫЙ_СТАРТ.md` → оставить как есть

**Публичная версия** (для GitHub):
- `ДОКУМЕНТАЦИЯ_PUBLIC.md` → версия без секретов
- `БЫСТРЫЙ_СТАРТ_PUBLIC.md` → версия без секретов

### Вариант 2: Замена в существующих файлах

Заменить реальные данные на плейсхолдеры:

```
10.100.200.198 → your_db_host
10.100.200.199 → your_ollama_host
cda_analytics → your_db_user
4HPzQt2HyU → your_db_password
chat_bot_db → your_database_name
```

---

## 📋 Checklist перед GitHub

### Обязательно:

- [x] `.gitignore` создан и содержит `.env`
- [x] `.env.example` создан (без реальных данных)
- [x] `benchmarks.example.json` создан
- [ ] Проверить все `.md` файлы на секреты
- [ ] Убрать реальные IP из `app/config.py` (опционально)
- [ ] Проверить что `.env` не в Git: `git status`

### Рекомендуется:

- [ ] Создать публичные версии документации
- [ ] Добавить LICENSE файл
- [ ] Добавить CONTRIBUTING.md
- [ ] Написать более общий README.md для публики

---

## 🔍 Проверка перед коммитом

```bash
# 1. Убедись что .env не в Git
git status | grep .env
# Должно быть пусто

# 2. Проверь что нет секретов в коде
grep -r "4HPzQt2HyU\|cda_analytics" app/ *.py
# Должно быть пусто

# 3. Проверь .gitignore
cat .gitignore | grep .env
# Должно быть: .env

# 4. Проверь что benchmarks.json не попадет в Git
git status | grep benchmarks.json
# Должно быть пусто
```

---

## 🚨 Что делать если случайно запушил секреты?

### Немедленные действия:

1. **Удалить из Git истории:**
```bash
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env" \
  --prune-empty --tag-name-filter cat -- --all

git push origin --force --all
```

2. **Сменить все пароли:**
- Пароль БД
- Учетные данные
- Пересоздать `.env`

3. **Уведомить команду:**
- Сообщить о инциденте
- Обновить документацию

---

## ✅ Безопасная публикация

### Для публичного GitHub:

1. Используй `.env.example` вместо `.env`
2. Используй `benchmarks.example.json`
3. Убери реальные IP/пароли из документации
4. Добавь общий README без деталей инфраструктуры

### Для приватного GitHub (внутри компании):

Можно оставить реальные примеры, но:
- Убедись что репозиторий **PRIVATE**
- Ограничь доступ только нужным людям
- Всё равно используй `.env` а не хардкод

---

## 📜 Лучшие практики

### ✅ Хорошо:

```python
# Использовать переменные окружения
DB_HOST = os.getenv("DB_HOST")
```

### ❌ Плохо:

```python
# Хардкод секретов
DB_HOST = "10.100.200.198"
DB_PASSWORD = "4HPzQt2HyU"
```

---

## 📞 Вопросы безопасности

Если нашел уязвимость или случайно выложил секреты:
1. Сразу удали из публичного доступа
2. Смени все пароли
3. Уведоми администратора БД
4. Проверь логи доступа

---

**🔒 Помни: Безопасность данных - это ответственность каждого!**
